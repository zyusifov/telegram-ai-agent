# AI-agent via telegram bot.

AI-agent integrated to telegram bot via telegram bot api also with using LangChain, as LLM used OpenAI.

```bash
cp .env-example .env
```

Install dependencies with package manager `uv`:

```bash
uv venv
uv sync
```

Run agent:

```bash
uv run main.py
```
